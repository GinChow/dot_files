# List of Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
# set -g @plugin 'ofirgall/tmux-window-name'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'erikw/tmux-powerline'
set -g @plugin 'imomaliev/tmux-peacock'
# # tmux continuum config
set -g @continuum-save-interval '1440' # Automatic saving session per day
set -g @continuum-restore 'on'
# # End contunuum config
# set -g default-command "reattach-to-user-namespace -l /bin/zsh"
# set -g default-terminal screen-256color
# set -g default-terminal xterm-256color
# set -g default-terminal screen-256color

# set -g @dracula-plugins "cpu-usage gpu-usage ram-usage"
# set -g @dracula-show-left-icon "#h | #S | #W"
# set -g @dracula-show-flags true
# set -g @dracula-show-edge-icons true
# set -g @dracula-show-powerline false

# 设置窗口状态格式，使用脚本动态获取颜色
# setw -g window-status-format '#[fg=#(~/.tmux/window-colors.sh "#W")]#I:#W'
# setw -g window-status-current-format '#[fg=#(~/.tmux/window-colors.sh "#W" "current"),bold]#I:#W'

# set-option -g '@peacock:hooks:session-style' "${XDG_CONFIG_HOME}/tmux/hooks/set_session_style.sh"
# set-option -g '@peacock:hooks:window-style' "~/.config/tmux/hooks/set_window_style.sh"

# 自动重命名window为当前目录名
set-option -g automatic-rename on
set-option -g automatic-rename-format '#{b:pane_current_path}'

set-option -ga terminal-overrides ",*256col*:Tc" # 这句是关键
set-option -s escape-time 10
set -g prefix C-q
set -g mouse on
set -g history-limit 5000

# Color
set -g status-style bg=black,fg=green
set-option repeat-time 200
unbind C-b
bind r source-file ~/.tmux.conf \; display "Reloaded!"
bind v split-window -h #split horizonal 
bind s split-window -v
bind-key C-q last-window

#up
bind-key -r k select-pane -U
#down
bind-key -r j select-pane -D
#left
bind-key -r h select-pane -L
#right
bind-key -r l select-pane -R

# resize pane
bind -r ^k resizep -U 5 # upward (prefix Ctrl+k)
bind -r ^j resizep -D 5 # downward (prefix Ctrl+j)
bind -r ^h resizep -L 5 # to the left (prefix Ctrl+h)
bind -r ^l resizep -R 5 # to the right (prefix Ctrl+l)

#copy-mode 将快捷键设置为vi 模式
setw -g mode-keys vi
bind [ copy-mode
# more settings to make copy-mode more vim-like
bind -Tcopy-mode-vi 'v' send -X begin-selection
bind -Tcopy-mode-vi 'C-v' send -X rectangle-toggle
bind -Tcopy-mode-vi 'y' send -X copy-pipe-and-cancel "tmux save-buffer - | reattach-to-user-namespace pbcopy"

# status bar theme - 更新为新语法
set -g status-style bg=colour235
set -g message-command-style fg=colour222
set -g status-justify 'centre'
set -g status-left-length '100'
set -g status 'on'
set -g pane-active-border-style fg=colour154
set -g message-style fg=colour222,bg=colour238
set -g status-right-length '100'
set -g status-right-style 'none'
set -g message-command-style fg=colour222,bg=colour238

# 删除UTF-8相关设置（新版本默认启用）
# set -g utf8 on
# set-window-option -g utf8 on
# set -g status-utf8 'on'

set -g status-style bg=colour235
set -g pane-border-style fg=colour238
set -g status-left-style 'none'
# setw -g window-status-style fg=colour121,bg=colour235
# setw -g window-status-activity-style fg=colour154,bg=colour235
# setw -g window-status-separator ''

# set -g status-left '#[fg=colour232,bg=colour154] #S #[fg=colour154,bg=colour238,nobold,nounderscore,noitalics]#[fg=colour222,bg=colour238] #W #[fg=colour238,bg=colour235,nobold,nounderscore,noitalics]#[fg=colour121,bg=colour235] #(whoami)  #(uptime  | cut -d " " -f 1,2,3) #[fg=colour235,bg=colour235,nobold,nounderscore,noitalics]'
# set -g status-right '#[fg=colour235,bg=colour235,nobold,nounderscore,noitalics]#[fg=colour121,bg=colour235] %r  %a  %Y #[fg=colour238,bg=colour235,nobold,nounderscore,noitalics]#[fg=colour222,bg=colour238] #H #[fg=colour154,bg=colour238,nobold,nounderscore,noitalics]#[fg=colour232,bg=colour154] #(rainbarf --battery --remaining --no-rgb) '
# setw -g window-status-format '#[fg=colour235,bg=colour235,nobold,nounderscore,noitalics]#[default] #I  #W #[fg=colour235,bg=colour235,nobold,nounderscore,noitalics]'
# setw -g window-status-current-format '#[fg=colour235,bg=colour238,nobold,nounderscore,noitalics]#[fg=colour222,bg=colour238] #I  #W  #F #[fg=colour238,bg=colour235,nobold,nounderscore,noitalics]'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
